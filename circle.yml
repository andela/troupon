machine:
  python:
    version: 2.7.10
  environment:
    DJANGO_SETTINGS_MODULE: troupon.settings.test
test:
  override:
    - python troupon/manage.py test --settings=troupon.settings.test
deployment:
  staging:
    branch: develop
    heroku:
      appname: troupon-staging
