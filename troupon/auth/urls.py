from django.conf.urls import url
# from views import ForgotPasswordView

urlpatterns = [
    # url(r'^auth/forgot_password/$', ForgotPasswordView.as_view(), name='Signup'),
    # url(r'^$', LandingPage.as_view(), name='homepage'),
]